- provide :title, "建立新品牌"
%section
  %header
    %h1= yield :title
  %section.brand_form
    = form_for @brand, validate: true do |f| 
      = render 'fields', f: f
      .action  
        = f.button "发布"
        = f.button "取消", type: "cancel"
- content_for :script do
  :javascript
    $(Pincool.brandForm);
