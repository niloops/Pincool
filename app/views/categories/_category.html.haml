%li
  .category
    %h2= link_to category.name, category
    %p= category_brands_tag category
    - if category.brands.count > 5
      .more= link_to "查看更多", category
  = render 'brands/brands', brands: category.brands.limit(5)
  
