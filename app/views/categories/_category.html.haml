%li
  .category
    %h2= link_to category.name, category
    %p= category_brands_tag category
    - if category.brands.count > 6
      = link_to "查看更多", category, "class" => "more"
  = render 'brands/brands', brands: category.brands.limit(6)
